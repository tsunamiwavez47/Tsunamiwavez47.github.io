

<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Be My Valentine</title>

<style>
body {
  background: pink;
  text-align: center;
  font-family: Arial, sans-serif;
  overflow: hidden;
}

h1 {
  margin-top: 60px;
  font-size: 2.5em;
}

p {
  font-size: 1.5em;
}

button {
  border: none;
  border-radius: 10px;
  cursor: pointer;
  position: absolute;
  transition: all 0.2s ease;
}

#yesBtn {
  background-color: red;
  color: white;
  left: 40%;
  top: 60%;
  padding: 15px 25px;
  font-size: 18px;
  z-index: 100;
}

#noBtn {
  background-color: gray;
  color: white;
  left: 55%;
  top: 60%;
  padding: 15px 25px;
  font-size: 18px;
  z-index: 50;
}

/* Love letter popup */
#letter {
  display: none;
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background: white;
  padding: 25px;
  border-radius: 15px;
  width: 300px;
  box-shadow: 0 0 20px rgba(0,0,0,0.3);
  z-index: 200;
}

#letter h2 {
  color: red;
}

.nextBtn {
  margin-top: 15px;
  background: red;
  color: white;
  padding: 8px 15px;
  border-radius: 8px;
}

/* Floating emojis */
.float {
  position: fixed;
  bottom: -10px;
  font-size: 24px;
  animation: floatUp 6s linear infinite;
}

@keyframes floatUp {
  0% { transform: translateY(0); opacity: 1; }
  100% { transform: translateY(-100vh); opacity: 0; }
}

/* I LOVE YOU animation */
#ui {
  position: fixed;
  inset: 0;
  pointer-events: none;
  display: none;
}

.love_word {
  position: absolute;
  top: 50%;
  left: 50%;
  color: #ea80b0;
  font-size: 1.4rem;
  text-shadow: 0 0 10px #fff;
  letter-spacing: 2px;
  white-space: nowrap;
  animation: swirl 6s linear infinite;
  animation-delay: calc(var(--i) * -0.2s);
}

@keyframes swirl {
  0% {
    transform: rotate(0deg) translate(0px) rotate(0deg);
    opacity: 1;
  }
  50% {
    transform: rotate(180deg) translate(150px) rotate(-180deg);
    opacity: .7;
  }
  100% {
    transform: rotate(360deg) translate(0px) rotate(-360deg);
    opacity: 0;
  }
}
</style>
</head>

<body>

<h1>Will you be my Valentine Melissa 🐇❤️</h1>
<p>From Brian 💌</p>

<button id="yesBtn" onclick="sayYes()">Yes</button>
<button id="noBtn">No</button>

<!-- Love Letter -->
<div id="letter">
  <h2>💌 My Love Letter</h2>
  <p>
    Dear Melissa,<br><br>
    Your crazy matches my crazy… and that’s one of the things I love about us. If love is a choice, then I choose you in the ways I can today, and I’ll keep choosing you when the day comes that we finally meet this year. You’re not just someone I like, you’re the woman I see a real future with. The one I want to build memories with, laugh with, grow with, and care for in a way that feels safe and genuine. And if you’ve ever wondered how I see you, I see you as the woman I want to cherish, not just for a moment, but for something meaningful and lasting. Your eyes and your smile are beautiful. They have a way of making my day better even through a screen. And this is the part where George Michael starts singing Careless Whisper if you said no 😉
    <br><br>— Brian
  </p>
  <button class="nextBtn" onclick="closeLetter()">Close</button>
</div>

<!-- Love text container -->
<div id="ui"></div>

<script>
const yesBtn = document.getElementById("yesBtn");
const noBtn = document.getElementById("noBtn");
const letter = document.getElementById("letter");
const ui = document.getElementById("ui");

let noClicks = 0;

function sayYes() {
  letter.style.display = "block";

  ui.style.display = "block";
  ui.innerHTML = "";

  for (let i = 0; i < 40; i++) {
    const span = document.createElement("span");
    span.className = "love_word";
    span.style.setProperty("--i", i);
    span.innerText = "I love you";
    ui.appendChild(span);
  }
}

function closeLetter() {
  letter.style.display = "none";
}

noBtn.addEventListener("mouseover", moveNo);
noBtn.addEventListener("click", noClicked);

function moveNo() {
  let maxX = window.innerWidth - noBtn.offsetWidth;
  let maxY = window.innerHeight - noBtn.offsetHeight;
  noBtn.style.left = Math.random() * maxX + "px";
  noBtn.style.top = Math.random() * maxY + "px";
}

function noClicked() {
  noClicks++;
  if (noClicks >= 10) sayYes();
  else moveNo();
}

/* Floating emojis */
setInterval(() => {
  const item = document.createElement("div");
  item.className = "float";
  const symbols = ["❤️","🐇","🌹"];
  item.innerHTML = symbols[Math.floor(Math.random()*symbols.length)];
  item.style.left = Math.random()*100 + "vw";
  item.style.fontSize = (Math.random()*20+20)+"px";
  document.body.appendChild(item);
  setTimeout(()=>item.remove(),6000);
},300);
</script>

</body>
</html>